import 'package:flutter/material.dart';

class Asistencias extends StatelessWidget {
  const Asistencias({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Asistencias'),
      ),
    );
  }
}