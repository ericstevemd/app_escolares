import 'package:flutter/material.dart';

class SubirNovedades extends StatelessWidget {
  const SubirNovedades({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('subir novedades'),
      ),
    );
  }
}